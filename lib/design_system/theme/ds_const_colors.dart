import 'package:flutter/material.dart';

class DSConstColors {
  static const Color blackFade = Color(0xFF000000);
  static const Color onBlackFade = Color(0xFFFFFFFF);

  static const Color success = Colors.blue;
  static const Color error = Colors.red;
}
