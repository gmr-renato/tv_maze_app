import 'package:flutter/material.dart';

class ListSeriesPage extends StatelessWidget {
  const ListSeriesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('List series page'),
      ),
    );
  }
}
