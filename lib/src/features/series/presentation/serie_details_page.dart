import 'package:flutter/material.dart';

class SerieDetailsPage extends StatelessWidget {
  const SerieDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Serie details page'),
      ),
    );
  }
}
